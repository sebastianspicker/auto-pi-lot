# @auto-pi-lot/cli

Local composition entry point. `npm run demo` prints a validated graph and initial ready nodes without model calls or task execution. Supervisor commands will be added in the executable vertical slice.
